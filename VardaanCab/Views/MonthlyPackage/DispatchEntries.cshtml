@model IEnumerable<VardaanCab.Models.DTO.MonthlyPackageEntryDTO>
@{
    ViewBag.Title = "Dispatched Vehicles";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<h2>Dispatched Vehicles</h2>

<div style="width:99%;margin-left:1%;margin-top:1%;margin-bottom:1%;height:400px;overflow:scroll">
    <div class="row r">
        <div class="col-md-12">
            <a class="btn btn-default" href="/MonthlyPackage/All?menuId=@ViewBag.menuId">Back</a>
        </div>
    </div>
    @if (Model != null && Model.Count() > 0)
    {
    <table class="table table-striped">
        <tr>
            <th>Vehicle</th>
            <th>Vehicle No</th>
            <th>St Date</th>
            <th>St Kms</th>
            <th>St time</th>
            <th>Updated by</th>
            <th>Update time</th>
            <th>Action</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.VehicleModel</td>
                <td>@item.CabNo</td>
                <td>@item.JourneyStartDate.ToString("dd-MMM-yyyy")</td>
                <td>@item.StKm</td>
                <td>@item.StartHr</td>
                <td>@item.UpdatedBy</td>
                <td>@item.UpdateDate.ToString("dd-MMM-yyyy")</td>
                <td>
                    <a href="@Url.Action("EditEntry", new { id=item.Id,menuId=@ViewBag.menuId})" class="btn btn-info">Edit</a>
                    <a href="@Url.Action("CloseDailyEntry",new { entryId=item.Id,menuId=@ViewBag.menuId})" class="btn btn-info">Close</a>
                </td>
            </tr>
        }
        </table>
    }
    else
    {
        <div class="row r">
            <div class="col-md-12">
                <h4>No Records found</h4>
            </div>
        </div>
    }
    </div>
